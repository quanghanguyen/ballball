<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ballball">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BallBall"
        tools:targetApi="31">
        <activity
            android:name=".UserInfomationActivity"
            android:exported="false" />
        <activity
            android:name=".onboarding.activity.OnBoardingActivity2"
            android:exported="false" />
        <activity
            android:name=".user.walkthrough.team.TeamActivity"
            android:exported="false" />
        <activity
            android:name=".user.walkthrough.avatar.AvatarActivity"
            android:exported="false" />
        <activity
            android:name=".user.walkthrough.name.NameActivity"
            android:exported="false" />
        <activity
            android:name=".login.phone.verify.SignInVerifyActivity"
            android:exported="false" />
        <activity
            android:name=".login.phone.login.SignInActivity"
            android:exported="false" />
        <activity
            android:name=".main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>